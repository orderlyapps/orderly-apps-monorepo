import { ModalProps } from "@amodeo/ui/ionic/use-card-modal/useCardModal";

export const {{ pascalCase name }} = ({ 
  children,
  modalProps,
}: { 
  children?: React.ReactNode, 
  modalProps: ModalProps;
}) => {
  return (
    <div className="full centered">
      <h1>{{ titleCase name }} Component</h1>
      {children}
    </div>
  );
};
