import {
  Document,
  Page,
  PDFDownloadLink,
  PDFViewer,
  Text,
  View,
  StyleSheet,
} from "@react-pdf/renderer";

type {{pascalCase name}}PDFData = {
  data?: any;
};

const styles = StyleSheet.create({
  page: {
    padding: 12,
    fontSize: 12,
  },
});

function {{pascalCase name}}PDF({ data }: {{pascalCase name}}PDFData) {

  return (
    <Document>
      <Page size={"A4"} style={styles.page}>
        <View>
          <Text>{{pascalCase name}} PDF</Text>
        </View>
      </Page>
    </Document>
  );
}

{{pascalCase name}}PDF.Download = ({
  children,
  data,
}: {
  children?: React.ReactNode;
} & {{pascalCase name}}PDFData) => (
  <PDFDownloadLink
    document={<{{pascalCase name}}PDF data={data} />}
    fileName="{{pascalCase name}}.pdf"
  >
    {children || "Download"}
  </PDFDownloadLink>
);

{{pascalCase name}}PDF.Render = ({ data }: {{pascalCase name}}PDFData) => {
  console.log("{{pascalCase name}}PDFData:", data);

  return (
    <div style={{ height: "80vh", width: "100vw" }}>
      <PDFViewer width={"100%"} height={"100%"}>
        <{{pascalCase name}}PDF data={data} />
      </PDFViewer>
    </div>
  );
};

export default {{pascalCase name}}PDF;