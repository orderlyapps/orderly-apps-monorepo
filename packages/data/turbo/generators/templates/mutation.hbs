/**
 * {{pascalCase name}}
 * 
 * @description Component for modifying {{name}} data
 */
import { useMutation, useQueryClient } from '@tanstack/react-query';
// Import query key if needed - assumes the query component has been generated
// import { {{camelCase name}}_query_key } from '../{{kebabCase name}}/{{pascalCase name}}';

/**
 * Execute {{name}} mutation
 * 
 * @param {object} params - The mutation parameters
 * @param {Record<string, any>} params.mutation_data - Data to send in the mutation
 * @returns {Promise<any>} The mutation result
 */
export const execute{{pascalCase name}} = async (params: { 
  mutation_data: Record<string, any>;
}): Promise<any> => {
  const { mutation_data } = params;
  
  // Replace with your actual API call logic
  const response = await fetch(`/api/{{kebabCase name}}`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(mutation_data),
  });
  
  if (!response.ok) {
    throw new Error('Failed to execute {{name}} mutation');
  }
  
  return response.json();
};

/**
 * {{pascalCase name}} component
 * 
 * @param {object} params - Parameters for the mutation
 * @param {object} params.options - Additional mutation options
 * @returns {UseMutationResult} The mutation result
 */
export default function {{pascalCase name}}(params: {
  options?: Parameters<typeof useMutation>[2];
} = {}) {
  const { options = {} } = params;
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: execute{{pascalCase name}},
    onSuccess: (data, variables) => {
      // Invalidate queries related to this data
      queryClient.invalidateQueries({ queryKey: ['{{camelCase name}}'] });
      
      // If there's a provided success callback, call it
      if (options.onSuccess) {
        options.onSuccess(data, variables, { revert: () => {} });
      }
    },
    ...options,
  });
}
